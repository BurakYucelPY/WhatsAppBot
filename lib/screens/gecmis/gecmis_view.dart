import 'package:flutter/material.dart';

class GecmisView extends StatelessWidget {
  const GecmisView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Geçmiş Sayfası'),
      ),
    );
  }
}
